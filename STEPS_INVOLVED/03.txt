Implementing setup.py to make our project a package itself for reusability provided by various platforms such as seaborn, numpy etc.
ANd, in order to do that we'll reqquire an forder comnating __init__.py placed inside the folder as source(src).
